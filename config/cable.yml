# Action Cable uses Redis by default to administer connections, channels, and sending/receiving messages over the WebSocket.
production:
  adapter: redis
  url: redis://localhost:6379/1
  channel_prefix: work_design_production

staging:
  adapter: redis
  url: redis://localhost:6379/1
  channel_prefix: work_design_staging

development:
  adapter: redis
  url: redis://localhost:6379/1
  channel_prefix: work_design_development

test:
  adapter: async
